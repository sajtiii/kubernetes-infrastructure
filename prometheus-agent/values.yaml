prometheus:
  serverFiles:
    prometheus.yml:
      rule_files: []

  alertmanager:
    enabled: false

  pushgateway:
    enabled: false

  kube-state-metrics:
    enabled: true
    resources:
      requests:
        cpu: 10m
        memory: 64Mi
      limits:
        cpu: 25m
        memory: 96Mi

  prometheus-node-exporter:
    enabled: true
    resources:
      requests:
        cpu: 10m
        memory: 32Mi
      limits:
        cpu: 25m
        memory: 48Mi

  prometheus-pushgateway:
    enabled: true

  configmapReload:
    prometheus:
      enabled: false

  server:
    revisionHistoryLimit: 1

    global:
      scrape_interval: 60s
      scrape_timeout: 10s
      evaluation_interval: 60s

    persistentVolume:
      enabled: false

    remoteWrite: [] # Should be set to the URL of the Prometheus server that will receive the data from this agent

    defaultFlagsOverride:
      - --agent
      - --config.file=/etc/config/prometheus.yml
      - --storage.agent.wal-compression
      - --storage.agent.retention.max-time=15m
      - --storage.agent.wal-truncate-frequency=5m
      - --web.console.libraries=/etc/prometheus/console_libraries
      - --web.console.templates=/etc/prometheus/consoles
      - --web.enable-lifecycle

    extraEnvs:
      - name: GOGC
        value: "75"
      - name: GOMEMLIMIT
        value: "110MiB"

    resources:
      limits:
        cpu: 200m
        memory: 128Mi
      requests:
        cpu: 50m
        memory: 64Mi