dashboard:
  hostname: ""
  allowedIps: []

vault:
  webhook:
    namespaceSelector:
      matchExpressions:
      - key: kubernetes.io/metadata.name
        operator: NotIn
        values:
        - kube-system
  csi:
    enabled: true
  server:
    enabled: true
    dataStorage:
      accessMode: ReadWriteMany
      storageClass: "" # Should be overwritten 
      size: 10G
    ingress:
      enabled: false
  ui:
    enabled: true
